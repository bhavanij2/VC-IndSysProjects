{
  "name": "pod-slack-alert-notifier",
  "version": "0.0.0",
  "description": "pod-slack-alert-notifier",
  "private": true,
  "jest": {
    "roots": [
      "test"
    ],
    "coverageReporters": [
      "text",
      "cobertura"
    ]
  },
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore .",
    "test": "jest",
    "coverage": "jest --coverage --coverageDirectory=coverage"
  },
  "dependencies": {
    "aws-sdk": "^2.404.0",
    "moment": "^2.24.0",
    "axios": "^0.19.0"
  },
  "devDependencies": {
    "eslint": "^5.14.1",
    "eslint-config-google": "^0.12.0",
    "jest": "^24.1.0",
    "jest-cli": "^24.1.0"
  }
}
