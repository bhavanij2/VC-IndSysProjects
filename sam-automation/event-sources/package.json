{
  "name": "migration-automation-sam",
  "version": "1.0.0",
  "description": "API Gateway Deployment Cloud Formation Custom Resource",
  "main": "index.js",
  "license": "UNLICENSED",
  "repository": "https://github.platforms.engineering/POD-Inc/vcis-cloudformation-templates",
  "jest": {
    "roots": [
      "test"
    ],
    "coverageReporters": [
      "text",
      "cobertura"
    ]
  },
  "scripts": {
    "test": "jest --env=node",
    "lint": "eslint .",
    "coverage": "jest --coverage --coverageDirectory=coverage",
    "postinstall": "install-local"
  },
  "dependencies": {
    "aws-sdk": "^2.566.0",
    "handlebars": "^4.5.3"
  },
  "devDependencies": {
    "eslint": "^6.5.0",
    "eslint-config-google": "^0.14.0",
    "jest": "^24.9.0",
    "jest-cli": "^24.9.0"
  },
  "localDependencies": {
    "migration-automation-shared": "../shared"
  }
}
